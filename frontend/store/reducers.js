import {
    TAB
} from "./actionType";

const initialTab = {
    tab : null
}

export const tabReducer = (state = initialTab, action) => {
//     switch (action.type) {
//         case 
//     }
    return {
        ...state,
        1
    }
}